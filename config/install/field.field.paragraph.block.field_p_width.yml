langcode: en
status: true
dependencies:
  config:
    - field.storage.paragraph.field_p_width
    - paragraphs.paragraphs_type.block
  module:
    - options
id: paragraph.block.field_p_width
field_name: field_p_width
entity_type: paragraph
bundle: block
label: Width
description: 'Specify a width for the paragraph component, going from tiny to full screen. For every option (except full screen) there is a variation where you can select an offset, making the entire paragraph span across the full width but with witespace at the beginning.'
required: true
translatable: true
default_value:
  -
    value: col-12
default_value_callback: ''
settings: {  }
field_type: list_string
